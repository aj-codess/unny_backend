

let sessionMap = new Map();


class session {
    constructor(id,session){
        this.online=false;
    }
}


let session_index = (id,session) => {
    try{

    } catch(error){

    };
};

export default session_index;